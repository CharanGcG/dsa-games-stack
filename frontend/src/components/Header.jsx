import styles from "../styles/AlterStack.module.css";

const Header = () => {
    return (
        <div>
            <h1 className={styles.header}>Alter Stack</h1>
        </div>
    )
};

export default Header;